import RegistrationContainer from "../containers/RegistrationContainer";

export default function RegistrationScreen(props) {
  const { navigation } = props;
  return <RegistrationContainer navigation={navigation} />;
}
