import RoomsListContainer from "../containers/RoomsListContainer";

export default function RoomsListScreen(props) {
  const { navigation } = props;
  return <RoomsListContainer navigation={navigation} />;
}
