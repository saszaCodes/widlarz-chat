import { cornerRadius } from ".";
import { white } from "./colors";

export const basicTextInput = {
  width: "100%",
  borderRadius: cornerRadius.smallRadius,
  backgroundColor: white,
  paddingVertical: 5,
  paddingHorizontal: 10,
};
